# Kiana Agent - ARK LLM Configuration Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# ARK LLM Configuration
# =============================================================================

# Your ARK API Key - Required for ARK models
# Get this from your ARK dashboard: https://ark.bytedance.com/
ARK_API_KEY=your-ark-api-key-here

# ARK Base URL - Usually https://ark.cn-beijing.volces.com/api/v3 for China region
# or https://ark.us-east-1.volces.com/api/v3 for US region
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# ARK Model ID - Choose from available models:
# - "doubao-pro-128k" (128k context, general purpose)
# - "doubao-pro-32k" (32k context, general purpose)  
# - "doubao-lite-128k" (128k context, faster/cheaper)
# - "doubao-lite-32k" (32k context, faster/cheaper)
ARK_MODEL_ID=doubao-pro-32k

# =============================================================================
# OpenAI Fallback Configuration (Optional)
# =============================================================================

# OpenAI API Key - Used as fallback if ARK is not configured
# Get this from https://platform.openai.com/
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI Model - Used when falling back to OpenAI
# - "gpt-4o-mini" (fast, cost-effective)
# - "gpt-4o" (most capable)
# - "gpt-3.5-turbo" (legacy, still good)
OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# Agent Configuration
# =============================================================================

# Maximum conversation rounds (default: 20)
MAX_ROUNDS=20

# Verbose logging (true/false)
VERBOSE=true

# Enable streaming mode (true/false)
STREAM=true

# =============================================================================
# Development Configuration
# =============================================================================

# Node environment
NODE_ENV=development

# Debug mode (true/false)
DEBUG=false

# =============================================================================
# Example Usage Instructions
# =============================================================================

# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and fill in your actual API keys and configuration
#
# 3. Test with a simple command:
#    node -e "const {runKianaV6} = require('./lib/KianaAgentV6'); runKianaV6({instruction: 'List current directory', arkConfig: {modelId: process.env.ARK_MODEL_ID, apiKey: process.env.ARK_API_KEY, baseURL: process.env.ARK_BASE_URL}}, require('./lib/MemTools').MemTools, require('./lib/Writer').BufferWriter).then(console.log).catch(console.error);"
#
# 4. Or use the interactive mode:
#    node bin/memsh --instruction "Analyze this codebase" --model doubao-pro-32k --ark-key $ARK_API_KEY --ark-base-url $ARK_BASE_URL
#
# 5. For streaming mode:
#    node bin/memsh --instruction "Show me the files" --stream --model doubao-pro-32k --ark-key $ARK_API_KEY --ark-base-url $ARK_BASE_URL

# =============================================================================
# ARK Model Information
# =============================================================================

# Doubao-Pro Series (High Performance):
# - doubao-pro-128k: 128,000 token context, best for long documents
# - doubao-pro-32k: 32,000 token context, good balance of performance and cost
# - doubao-pro-4k: 4,000 token context, fastest response, lowest cost
#
# Doubao-Lite Series (Cost Effective):
# - doubao-lite-128k: 128,000 token context, faster than pro, lower cost
# - doubao-lite-32k: 32,000 token context, good for most tasks
# - doubao-lite-4k: 4,000 token context, fastest and cheapest
#
# Specialized Models:
# - doubao-pro-functioncall-32k: Optimized for function calling
# - doubao-vision-pro-32k: Vision-capable model

# =============================================================================
# Troubleshooting
# =============================================================================

# If you get "API key missing" errors:
# - Make sure ARK_API_KEY is set correctly
# - Verify the ARK_BASE_URL matches your region
# - Check that your ARK account has API access enabled
#
# If you get "model not found" errors:
# - Verify ARK_MODEL_ID is available in your region
# - Check the ARK documentation for available models
# - Try a different model ID
#
# If you get network errors:
# - Verify ARK_BASE_URL is correct for your region
# - Check your network connectivity
# - Try the fallback OpenAI configuration

# =============================================================================
# Security Notes
# =============================================================================

# - Never commit your actual .env file to version control
# - Keep your API keys secure and rotate them regularly
# - Use environment-specific API keys (dev/staging/prod)
# - Monitor your API usage and costs in the ARK dashboard
# - Set up spending limits in your ARK account to prevent unexpected charges